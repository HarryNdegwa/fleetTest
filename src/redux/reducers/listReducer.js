const initialState = {
  list: null,
  starred: [],
  normal: [],
  checked: [],
  persistedList: null,
};

export const listReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
